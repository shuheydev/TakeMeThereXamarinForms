<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="TakeMeThereXamarinForms.Views.SelectTargetPage">


    <AbsoluteLayout>
        <ListView AbsoluteLayout.LayoutFlags="All"
                  AbsoluteLayout.LayoutBounds="0,0,1,1"
                  BackgroundColor="#2EFE2E88"
                  ItemsSource="{Binding Targets}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell Height="200">
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding PlusCode}" FontSize="80" HeightRequest="50"/>
                            <Label Text="{Binding Name}" FontSize="25" HeightRequest="30"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="+" FontSize="50"
                AbsoluteLayout.LayoutFlags="PositionProportional"
                AbsoluteLayout.LayoutBounds="0.9,0.95,70,70" 
                CornerRadius="35" BackgroundColor="Green"
                Command="{Binding NavigateCommand}" CommandParameter="TargetDetailPage"/>
    </AbsoluteLayout>
</ContentPage>